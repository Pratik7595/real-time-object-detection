# Normalise line endings in the repository to LF regardless of who commits.
# This project claims to be platform-independent; a repo that hands Linux
# reviewers CRLF shebangs and shell scripts is not.
* text=auto eol=lf

# Never touch binaries.
*.png binary
*.jpg binary
*.onnx binary
*.mp4 binary
